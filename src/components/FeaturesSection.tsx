
import React from 'react';

interface FeaturesSectionProps {
  language: 'en' | 'es';
}

export const FeaturesSection = ({ language }: FeaturesSectionProps) => {
  // This component is now empty as requested by the user
  return null;
};
